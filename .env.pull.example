# Local-only config for `make db-pull-live` (do not commit).
#   cp .env.pull.example .env.pull

VPS_HOST=YOUR_HETZNER_SERVER_IP
VPS_SSH_USER=root
# VPS_SSH_KEY=$HOME/.ssh/id_ed25519_hetzner
# VPS_APP_DIR=/var/www/premier-league-tournament
